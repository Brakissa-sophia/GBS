{% extends 'base.html.twig' %}



{% block body %}
   <h2>{{ skin_type.title|capitalize }}</h2>

    <a class="btn btn-dark" href="{{ path('app_skin_type_index') }}">Gestion type de peau</a>

    <table class="table table-striped table-hover table-bordered text-center mt-3" class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ skin_type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ skin_type.title }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('app_skin_type_edit', {'id': skin_type.id}) }}">edit</a>

    {{ include('skin_type/_delete_form.html.twig') }}
{% endblock %}
