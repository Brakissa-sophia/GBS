{% extends 'base.html.twig' %}



{% block body %}
     <h2>Modifier type de peau" {{skin_type.title|capitalize}}"</h2>

      <a class="btn btn-dark" href="{{ path('app_skin_type_index') }}">Gestion type de peau</a>

    {{ include('skin_type/_form.html.twig', {'button_label': 'Modifier type de peau'}) }}


    {{ include('skin_type/_delete_form.html.twig') }}
{% endblock %}
