{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Ajouter une marque{% endblock %}
{% block description %}{{ parent() }} | Ajouter une marque{% endblock %}
{% block keywords %}{{ parent() }} | Ajouter une marque{% endblock %}

{% block body %}
<h2>Ajouter une marque</h2>

<div class="d-flex gap-2 mb-3">
    <a class="btn btn-dark" href="{{ path('app_brand_index') }}">
        <i class="bi bi-arrow-left"></i> Retour
    </a>
</div>

<div class="row mt-3">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header bg-primary">
                <h5 class="mb-0 text-dark">Nouvelle marque</h5>
            </div>
            <div class="card-body">
                {{ include('brand/_form.html.twig') }}
            </div>
        </div>
    </div>
</div>

{% endblock %}