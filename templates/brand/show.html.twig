{% extends 'base.html.twig' %}



{% block body %}

<h2>{{ brand.title|capitalize }}</h2>

    <a class="btn btn-dark" href="{{ path('app_brand_index') }}">Gestion des marques</a>

    <table class="table table-striped table-hover table-bordered text-center mt-3">
        <tbody>
            <tr>
                <th class="w-25">Id</th>
                <td class="w-75">{{ brand.id }}</td>
            </tr>
            <tr>
                <th class="w-25">Titre</th>
                <td class="w-75">{{ brand.title }}</td>
            </tr>
            
        </tbody>
    </table>
    

    

    <a href="{{ path('app_brand_edit', {'id': brand.id}) }}">edit</a>

    {{ include('brand/_delete_form.html.twig') }}
{% endblock %}
